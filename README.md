# ВЕБ ПРИЛОЖЕНИЕ YOLK

## стек

- client: React
- server: ExpressJs
- docker/nginx

### Как запускать

Для прода

```bash
docker-compose -f docker-compose.prod.yml up --build
```

Для разработки

```bash
docker-compose up --build
```